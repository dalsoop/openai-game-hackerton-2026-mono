# server-board

모든 `apps/server-*` 슬롯을 한 화면에서 본다.

주소: `https://server-board.external.kr/`

카탈로그는 `python3 deploy/scripts/plant-apps.py` 가 `project/web/slots.json` 에 쓴다.
클러스터에 올리려면 `python3 deploy/scripts/apply-apps.py`.
