apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hackertone-games.fullname" . }}-dagul-game
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hackertone-games.labels" . | nindent 4 }}
    grafana_dashboard: "1"
    app.kubernetes.io/component: grafana
data:
  dagul-game.json: |-
{{ .Files.Get "files/dagul-game-dashboard.json" | indent 4 }}
