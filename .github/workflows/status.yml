name: Status

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  catalog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Plant catalog
        run: python3 deploy/scripts/plant-apps.py
